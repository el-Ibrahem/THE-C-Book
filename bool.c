#include <stdlib.h>
#include <stdio.h>
#include <stdbool.h>
/*
[bool determines if the inside it is true or false any non zero number inside bool is True]
bool arm_detonator(int);

void arm_missle(void){
    bool armed = arm_detonator(3);
    if (armed) puts("missile armed");
    else puts("missile disarmed");
}
*/

int main(){

}